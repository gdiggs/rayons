version: 2
jobs:
  build:
    docker:
      - image: cimg/base:2020.05
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Run tests
          command: |
            touch .env
            docker-compose up -d
            make test
